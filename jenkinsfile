#!/usr/bin/env groovy

/*
    Declerative pipeline for Jenkins builds using a shared library
    for any internal build logic.
*/
@Library('xap-common@dependency-check') _

xap {
    java_version = 'Java8'
    debug = true
}